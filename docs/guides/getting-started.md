# Getting started

Example user password in docs: `test-password` (never realistic phrases).

| Field | Example |
|-------|---------|
| email | `user@example.com` |
| password | `test-password` or a bcrypt hash |
